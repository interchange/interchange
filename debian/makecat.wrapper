#! /bin/sh -e

. /etc/interchange/init.cfg
OPTS='--nocfg --norunning --linkprogram=/usr/lib/cgi-bin/ic/vlink'
if [ "$USER" = "`id -un`" ]; then
	/usr/lib/interchange/bin/makecat -c /etc/interchange/makecat.cfg $OPTS $@
else
	/usr/lib/interchange/bin/makecat -c makecat.cfg $OPTS $@
fi
