#! /bin/sh -e

. /etc/interchange/init.cfg
OPTS="--log /var/log/interchange/error.log --confdir /var/run/interchange"
if [ "$UID" -eq 0 ]; then
	su -s /bin/sh -c "/usr/lib/interchange/bin/interchange $OPTS $@" $USER
else
	/usr/lib/interchange/bin/interchange $OPTS $@
fi