# fcounter

`[fcounter]` is an alias of [counter](counter.md). It takes the same
attributes and behaves identically; see [counter](counter.md) for the full
reference.

    [fcounter file=counter.basic start=10]

## Source

Defined as an `Alias` of `counter` in `code/SystemTag/counter.coretag`.
