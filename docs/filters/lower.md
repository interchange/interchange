# lower

`lower` is an alias for the [lc](lc.md) filter, which converts the entire input
to lower case.

See [lc](lc.md) for full documentation.

## Source

Defined as an alias in `code/Filter/lc.filter`
(`CodeDef lower Alias lc`).
